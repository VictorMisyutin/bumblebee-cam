[Service]
# /run/pollinator is tmpfs — RAM, not SD.
# Preserve it: the field UI bind-mounts this directory, and recreating it
# on every restart leaves that mount pointing at a deleted inode.
RuntimeDirectory=pollinator
RuntimeDirectoryMode=0755
RuntimeDirectoryPreserve=yes
